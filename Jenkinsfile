@Library('linkurious-shared')_

nodeJob {
  // project name on github
  projectName = "linkurious/valcheck"
  // publish on our public npm when releasing
  runNpmPublish = true
  // create a giot tag for each release
  createGitTag = true
  // bump version on develop after releasing
  runBookeeping = true
}
